# BillMind web — build-time config (Vite inlines VITE_* into the client bundle,
# so only put PUBLIC values here). Copy to `.env.local` for dev.

# Google OAuth *web* client ID (Google Cloud Console → Credentials → OAuth
# client → Web application). Must match the server's GOOGLE_CLIENT_ID audience.
VITE_GOOGLE_CLIENT_ID=
