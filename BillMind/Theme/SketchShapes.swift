import SwiftUI

// Voyage geometry is clean: cards/buttons use SwiftUI's RoundedRectangle
// (see SketchCardModifier). The old hand-drawn `WobblyRoundedRectangle` was
// unused and has been retired for the Voyage redesign.
